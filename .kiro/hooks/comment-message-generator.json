{
  "version": "v1",
  "hooks": [
    {
      "name": "Comment Message Generator",
      "trigger": "PostTaskExec",
      "action": {
        "type": "command",
        "command": "git status\ngit diff --cached"
      },
      "description": "Generate a Conventional Commits style Git commit message from the staged changes.",
      "timeout": 30,
      "enabled": true
    }
  ]
}
