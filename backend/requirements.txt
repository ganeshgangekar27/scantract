# ScanTract Backend Dependencies

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # For file uploads

# Database & ORM
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0  # PostgreSQL async driver
alembic>=1.12.0  # Database migrations
pgvector>=0.2.3  # PostgreSQL vector extension support

# Pydantic (data validation)
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Document Processing
PyMuPDF>=1.23.0  # PDF processing
python-docx>=1.0.0  # DOCX processing
paddleocr>=2.7.0  # OCR for scanned documents
paddlepaddle>=2.5.0  # PaddleOCR dependency

# LLM & RAG
langchain>=0.1.0  # Prompt templates and LLM orchestration
openai>=1.3.0  # OpenAI API (GPT)
anthropic>=0.7.0  # Anthropic API (Claude)
google-generativeai>=0.3.0  # Google Gemini API

# Environment & Configuration
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For FastAPI test client

# Utilities
python-jose[cryptography]>=3.3.0  # JWT tokens (if adding auth later)
passlib[bcrypt]>=1.7.4  # Password hashing (if adding auth later)
